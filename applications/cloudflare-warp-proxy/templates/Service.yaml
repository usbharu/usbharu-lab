apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-cloudflare-warp-service
spec:
  selector:
    app: {{ .Release.Name }}-cloudflare-warp
  ports:
    - protocol: TCP
      port: 40000
  